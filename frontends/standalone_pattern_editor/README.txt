The pattern editor in a standalone format.
See Readme.txt in ../dependencies for dependencies needed to build.

Build using make
make -f targets/linux/Makefile
make -f targets/macos/Makefile

For windows see vs files in targets/mcvs.
